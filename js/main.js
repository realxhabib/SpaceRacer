/**
 * Main entry point for Space Racer game
 */

// Wait for DOM to be fully loaded
document.addEventListener('DOMContentLoaded', () => {
    // Create and initialize game
    const game = new Game();
    
    // Log initialization
    console.log('Space Racer initialized');
});
